# Eval: ahe-concept-shaping

## 保护的行为 contract

1. 给定 opportunity，能发散至少 3 个有实质差异的概念方向
2. 对每个方向执行 commodity challenge（不只是赞美）
3. 通过多 agent PK 收敛为推荐 wedge
4. 若无 opportunity 工件，应 reroute 到 `ahe-opportunity-mapping`

## 运行方式

```bash
# 使用 evals.json 中的 prompt 分别运行 with_skill 和 without_skill
# 对比输出是否满足 assertions
```
