# Eval: ahe-outcome-framing

## 保护的行为 contract

1. 给定模糊 idea，能重写为 outcome 语言（目标用户 + 情境 + 替代品 + outcome）
2. 能识别并显式写出 commodity 风险
3. 至少生成 3 个候选 framing（pain-first / progress-first / wedge-first）
4. 不停留在 feature 列表层面

## 运行方式

```bash
# 使用 evals.json 中的 prompt 分别运行 with_skill 和 without_skill
# 对比输出是否满足 assertions
```
