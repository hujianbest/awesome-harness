# Eval: ahe-spec-bridge

## 保护的行为 contract

1. 给定多个上游工件，能压缩为一句话 opportunity thesis
2. 区分 evidence-backed 内容和 working hypothesis
3. 给出有边界的 v1 scope（含非目标）
4. 不把 concept brief 直接冒充 spec

## 运行方式

```bash
# 使用 evals.json 中的 prompt 分别运行 with_skill 和 without_skill
# 对比输出是否满足 assertions
```
