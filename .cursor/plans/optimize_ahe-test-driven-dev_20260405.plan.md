---
name: optimize ahe-test-driven-dev
overview: 提升 `ahe-test-driven-dev` 的测试设计检查和 fresh evidence 纪律，重点补强测试设计 checklist、当前轮验证要求与实现完成前的证据表达，同时保持其作为唯一实现入口的契约不变。
---

# 优化 `ahe-test-driven-dev` 方案

## 目标

让 `ahe-test-driven-dev` 更稳定地以单任务、fail-first、fresh evidence 的方式推进实现。
