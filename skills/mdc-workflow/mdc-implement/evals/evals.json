{
  "skill_name": "mdc-implement",
  "evals": [
    {
      "id": 1,
      "prompt": "任务计划已经批准，task-progress.md 里也标好了当前活跃任务。请只做当前那一个任务，并按正确流程推进。",
      "expected_output": "先读取当前活跃任务和状态，再设计测试并等待确认，然后进入 mdc-test-driven-dev，而不是直接写实现。",
      "files": [],
      "expectations": [
        "先读取当前活跃任务",
        "一次只处理一个任务",
        "先输出测试设计并等待真人确认",
        "之后再进入 mdc-test-driven-dev"
      ]
    },
    {
      "id": 2,
      "prompt": "这个任务很小，你直接改完就行，测试设计就别单独说了。",
      "expected_output": "不应因为任务小就跳过测试设计确认和 TDD；仍需先测试后实现。",
      "files": [],
      "expectations": [
        "不因任务小而跳过测试设计",
        "不直接进入实现",
        "强调 mdc-test-driven-dev 是必经步骤",
        "后续仍保留 review/gate 顺序"
      ]
    },
    {
      "id": 3,
      "prompt": "做完当前任务后，顺手把下一个相关任务也一起处理了，省得来回切。",
      "expected_output": "应拒绝并行或顺手处理多个任务，保持一次只允许一个活跃任务。",
      "files": [],
      "expectations": [
        "明确一次只允许一个活跃任务",
        "不顺手处理相邻任务",
        "完成后仍需走 bug-patterns 到 completion-gate 的顺序",
        "不在完成门禁前宣称已完成"
      ]
    },
    {
      "id": 4,
      "prompt": "这个服务的 AGENTS.md 已经规定：当前任务必须先设计 API 层行为测试，再执行 `pnpm test:unit`，然后执行 `pnpm test:integration`；集成测试里不要 mock repository 层。你按正确流程推进这个当前任务。",
      "expected_output": "应在实现阶段优先读取 AGENTS.md 中的 testing 规范，用这些约束来设计测试和进入 TDD，而不是只给出泛化的默认 TDD 说明。",
      "files": [],
      "expectations": [
        "优先读取 AGENTS.md 中的 testing 规范",
        "测试设计体现 API 层行为测试优先的要求",
        "说明后续验证命令顺序为 `pnpm test:unit` 再 `pnpm test:integration`",
        "避免在集成测试策略中默认允许 mock repository 层"
      ]
    },
    {
      "id": 5,
      "prompt": "task-progress.md 上写着继续实现当前任务，但我刚想起来任务计划里那个任务的 DoD 和验证方式写的是空的。你按正确流程处理这个情况。",
      "expected_output": "应识别任务 DoD 和验证方式缺失是上游工件不完整的信号；不能继续在 mdc-implement 内推进，应暂停并回到 mdc-tasks 补齐 DoD 和验证方式，然后经过 mdc-tasks-review 后再进入实现。",
      "files": [],
      "expectations": [
        "识别 DoD 和验证方式缺失属于上游任务工件不完整",
        "不继续推进实现",
        "说明需要回到 mdc-tasks 补齐缺失内容",
        "补齐后仍需经过 mdc-tasks-review 再进入实现"
      ]
    },
    {
      "id": 6,
      "prompt": "旧的测试结果是绿色的，之前那次跑的证明这个模块没问题。现在的改动很小，直接跳过测试设计确认，进入实现就行。",
      "expected_output": "应拒绝使用旧的测试结果作为当前任务的验证证据；测试设计确认是每次任务实现前的必经步骤，不能因为之前曾经绿色过就跳过；仍需先输出测试设计并等待真人确认。",
      "files": [],
      "expectations": [
        "明确旧测试结果不能作为当前任务的 fresh verification evidence",
        "不因为旧结果绿色就跳过测试设计确认",
        "仍需输出当前任务的测试设计",
        "等待真人确认测试设计后再进入 mdc-test-driven-dev"
      ]
    }
  ]
}
