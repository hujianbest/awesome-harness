# Code Review — F016 r1 (Memory Activation)

- **日期**: 2026-04-27
- **审阅人**: Cursor Agent (auto-streamlined per F011-F015 mode)
- **范围**: T1-T4 src 修改 (3 新模块 + 5 CLI handler + 3 STYLE 模板 + 4 sentinel)

## Verdict: APPROVED

## 改动摘要

| 模块 | 增量 LOC | 变更类型 |
|---|---|---|
| `src/garage_os/memory_activation/__init__.py` | +20 (新) | 顶级包 |
| `src/garage_os/memory_activation/types.py` | +50 (新) | MemoryStatus + IngestSummary dataclass |
| `src/garage_os/memory_activation/templates.py` | +60 (新) | parse_style_template + SUPPORTED_LANGUAGES |
| `src/garage_os/memory_activation/ingest.py` | +280 (新) | 3 ingest paths + dedup + dry-run + strict |
| `src/garage_os/cli.py` | +180 | memory subparser (4 sub) + init prompt + status 集成 + 5 handler |
| `packs/garage/templates/style-templates/{python,typescript,markdown}.md` | +90 (新) | 3 STYLE 模板 (22 entries) |

**总增量**: ~680 LOC src + 90 LOC templates + 4 sentinel test files

## 设计-代码对齐性 (检查 ADR-D16-1..6)

- ✓ ADR-D16-1 (顶级包): `src/garage_os/memory_activation/` 与 sync/skill_mining/workflow_recall/agent_compose 平级
- ✓ ADR-D16-2 (init prompt 不重载 --yes): `_init` 决策矩阵 — `--no-memory` → false; `--yes` (无 --no-memory) → false (Cr-1 r2 critical); interactive TTY → prompt; non-TTY 无 flag → false
- ✓ ADR-D16-3 (ingest dedup): source_evidence_anchors[].review_path / commit_sha / topic uniqueness
- ✓ ADR-D16-4 (review parser): `_REVIEW_FILENAME_RE` regex 5 kinds × {review,gate} × `f<NNN>` × `r<R>` × `<date>`
- ✓ ADR-D16-5 (git log parser): `subprocess.run(["git", "log", "--oneline", f"-{limit}"])` + regex `f\d+\(` + `hf-[a-z-]+`
- ✓ ADR-D16-6 (T1-T4 1:1): commits e671070 (T1) / 88fa62d (T2) / a15a861 (T3) / 当前 (T4) 与 design § 8 1:1

## 代码质量

- 类型注解: 完整 (新代码 100% type-annotated; dataclass)
- Docstring: 全部公共函数有 docstring 引 FR + spec/Cr-N r2 修订记录
- 异常处理: subprocess git log catch FileNotFoundError + CalledProcessError; review parsing strict / non-strict 分支
- I/O abstraction: stderr injection for test isolation
- B5 user-pact: --yes 不重载 memory (CRITICAL Cr-1 r2 守门); --dry-run / --strict opt-in; ingest 默认 strict=False (skip + log warn)
- 无 dead code, 无 commented-out 代码
- ruff baseline diff: **0**

## 红线确认

- ✓ CON-1601 (F003-F015 既有 API 字节级): 仅修改 cli.py (subparser/handler/init prompt/status 集成); 不动 memory/ knowledge/ ingest/ skill_mining/ workflow_recall/ agent_compose/ runtime/ 等任何模块
- ✓ CON-1602 (零依赖): `git diff main..HEAD -- pyproject.toml uv.lock` = 0
- ✓ CON-1603 (perf): ingest 单测覆盖 N=2 reviews / 3 commits / 7 STYLE entries 速度极快
- ✓ CON-1604 (F009 init flags 不动): 仅加 `--no-memory` 新 sibling flag
- ✓ CON-1605 (F015 agent compose 不动): F016 经 KnowledgeStore.store 写 STYLE; F015 list_entries(KnowledgeType.STYLE) 自动看到

## 残余项

- **None blocking**. F011-pattern E402/I001/F841 ruff 错误是 pre-existing 全局模式, F016 引入 0 新错误.

## 通过条件

✅ code review APPROVED, 进入 traceability review.
