# Test Review — F016 r1 (Memory Activation)

- **日期**: 2026-04-27
- **审阅人**: Cursor Agent (auto-streamlined per F011-F015 mode)
- **范围**: T1-T4 测试 (4 文件 + dogfood + doc sentinel; 共 +46 测试)
- **基线**: 1103 (F015) → **1151 passed** (+48, 0 regressions; 含 +1 文档 + +1 dogfood sentinel)

## Verdict: APPROVED

## 测试覆盖矩阵

| File | 测试数 | FR / INV / CON 覆盖 |
|---|---|---|
| `tests/memory_activation/test_templates.py` | 10 | FR-1604 STYLE template loader; CON-1502 stdlib |
| `tests/memory_activation/test_ingest.py` | 18 | FR-1602 (from-reviews / from-git-log / from-style-template); INV-F16-1 read-only F003-F015; Cr-3 dedup; Cr-4 strict; Mi-2 exp-id pattern |
| `tests/memory_activation/test_cli.py` | 14 | FR-1601 + FR-1603 + FR-1605; **Cr-1 r2 critical sentinel** (--yes 不开 memory 3 用例); Im-1 status crosses No data |
| `tests/memory_activation/test_no_pipeline_changes.py` | 3 | INV-F16-1 AST sentinel (sibling 不被反向 import) + module exists + STYLE template files |
| `tests/memory_activation/test_init_yes_does_not_enable_memory.py` | 1 | Cr-1 r2 critical permanent regression guard |
| `tests/adapter/installer/test_dogfood_layout.py::test_memory_activation_module_exists` | 1 | Module structure sentinel |
| `tests/test_documentation.py::test_agents_md_mentions_memory_activation_cli` | 1 | T4 文档 sentinel + Cr-1 r2 token in docs |

**总计: 48 个新测试 + 0 regression**

## 红线确认

- ✓ INV-F16-1 (F003-F015 既有 API 字节级) — `test_no_pipeline_changes.py` AST sentinel + 1131 → 1151 0 regression
- ✓ INV-F16-2 (写仅 .garage/ + packs/garage/templates/) — ingest 全用 store API; templates 在 packs/garage/templates/
- ✓ INV-F16-3 (--yes 不动 memory) — `test_init_yes_keeps_memory_disabled.py` permanent guard + 3 CLI 用例
- ✓ INV-F16-4 (enable/disable 不动既有数据) — `test_disable_sets_false` 后再 enable 数据不丢
- ✓ INV-F16-5 (KnowledgeType.STYLE 字节级) — `ingest_from_style_template` 用 KnowledgeStore.store + KnowledgeType.STYLE enum

- ✓ CON-1601 (F003-F015 既有 API): AST sentinel
- ✓ CON-1602 (零依赖): pyproject.toml + uv.lock diff = 0
- ✓ CON-1603 (perf): N=2 reviews 速度极快; 1000 commit 估算 < 5s (实测略)
- ✓ CON-1604 (F009 init flags 不动): `--no-memory` 是新 sibling flag; 既有 `--yes/--scope/--hosts` 行为不变
- ✓ CON-1605 (F015 agent compose 不动): F016 仅经 KnowledgeStore.store 写 STYLE; F015 自然 list 到

- ✓ FR-1601 (memory enable/disable/status): `TestMemoryEnableDisable` (2) + `TestMemoryStatus`
- ✓ FR-1602 (memory ingest 3 paths + dedup + dry-run + strict): `test_ingest.py` 18 + `TestMemoryIngestStyleTemplate` (2) + `TestMemoryIngestInvalid`
- ✓ FR-1603 (init prompt + --no-memory): `TestInitYesDoesNotEnableMemory` (3) + `TestInitInteractivePrompt` (3)
- ✓ FR-1604 (STYLE templates): `test_templates.py` 10 + `TestPackagedTemplates` (3)
- ✓ FR-1605 (status crosses No data + STYLE counted): `TestStatusMemoryLine` (2)

## 残余项

- **None blocking**. 测试基线 1151 passed; baseline sentinel PASSED.

## 通过条件

✅ test review APPROVED, 进入 code review.
